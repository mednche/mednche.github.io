

```python
import imp
#imp.reload(IncidentFramework)
#imp.reload(AgentFramework2)
imp.reload(GAN_model)

gan = GAN_model.CGAN_Entity_Embedding(data, labels) 
#gan = WCGANNumerical(data, cat_column)
G_loss, D_loss = gan.train(data, labels, epochs=200, save_model_interval=10)
```
